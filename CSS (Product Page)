body {
    margin: 0;
    padding: 0;
    font-family: 'Arimo', sans-serif;
    background-color: #F7F2DE;
    color: #0B0A08;
    line-height: 1.6;
}

/* Header */
header {
    background-color: #F7F2DE;
    display: flex;
    justify-content: center; /* Logo in the middle */
    align-items: center;
    padding: 0px 5px;
    position: relative;
    border-bottom: 1px solid #000000;
}

.logo {
    font-family: 'Frank Ruhl Libre', serif;
    font-size: 32px;
    text-align: center;
}

.burger-menu {
    font-size: 24px;
    background: none;
    border: none;
    cursor: pointer;
    position: absolute;
    left: 20px; /* Burger on upper left of screen */
    top: 50%;
    transform: translateY(-50%); 
}

.cart-icon {
    font-size: 14px;
    color: black; 
    background-color: #28BD6D;
    width: 47px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    cursor: pointer;
    position: absolute;
    right: 20px; 
    top: 50%;
    transform: translateY(-50%);
}

/* Burger Icon Hover */
.burger-menu:hover {
    opacity: 0.4; /* 40% opacity */
    transition: opacity 0.3s ease;
}


.product-title {
    font-family: 'Arimo', sans-serif;
    font-size: 36px;
    font-weight: normal;
    text-transform: uppercase; 
    padding: 40px;
    margin-bottom: 0px;
}

.product-description {
    font-family: 'Arimo', sans-serif;
    font-size: 20px;
    padding: 0 40px;
    margin-top: 0px;
    justify-content: left;
    max-width: 40%; 
    text-align: left; 
    line-height: 1.3;
}


.product-list {
    padding: 20px;
    border-bottom: 1px solid #000000;
    
}

.product-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); 
    gap: 20px; 
    justify-items: center;
}

/* Product Card */
.product-card {
    width: 454px; /* Fixed width for larger screens */
    height: auto; 
    border-radius: 16px;
    position: relative;
    overflow: hidden;
}

/* Image Container */
.image-container {
    position: relative; 
    width: 100%;
    height: 432px;
}

.product-image {
    width: 100%;
    height: 100%;
    object-fit: cover; 
}

/* Add to Basket Button */
.add-to-basket {
    position: absolute;
    bottom: 10px;
    right: 10px;
    background-color: #000000;
    color: #FFFFFF;
    font-size: 14px;
    padding: 8px 16px;
    border-radius: 20px;
    border: none;
    cursor: pointer;
    opacity: 0.85;
    z-index: 2; /* For button to stay on top of the image */
    transition: opacity 0.3s ease;
}

.add-to-basket:hover {
    opacity: 1;
}

/* Product Info Section */
.product-info {
    margin-top: 10px; 
    text-align: left;
}

/* Product Details */
.product-details {
    display: flex;
    justify-content: space-between; /* Name on left, price on right */
    font-size: 24px;
    font-weight: normal;
}

.product-weight {
    font-size: 20px;
    color: #777;
    margin-top: 5px;
}

